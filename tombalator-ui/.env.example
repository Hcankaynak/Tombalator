# API Base URL
# For local development, use: http://localhost:3000
# For production (Docker), leave empty to use nginx proxy
VITE_API_URL=http://localhost:3000

# WebSocket URL (optional)
# For local development, use: ws://localhost:3000
# For production (Docker), leave empty to auto-detect from window.location
VITE_WS_URL=
